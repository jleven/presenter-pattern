#with this Gemfile, I am able to run `rake test` from RubyMine and take advantage of the nice interface

source 'http://rubygems.org'

group :development, :test do
  gem 'rails', '3.0.7'
  gem 'test-unit'
  gem 'echoe'
  gem 'sqlite3'
  gem 'presenter-pattern', :require => 'presenter-pattern', :path => '.'
end
